# Bundled assets

This directory is intentionally source-only in `v0.1`. Add only redistribution-safe templates, themes, or placeholders here. User masters, extracted assets, generated images, and private gold-standard files belong in per-run directories or `tests/private-fixtures/`, never in the published plugin.
